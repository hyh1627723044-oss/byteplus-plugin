# 首版验证记录

日期：2026-09-20。Windows，Node.js 22.17.0，Python 3 标准库；无安卓设备连接。

- `npm run check`：26/26 测试通过，ToolPkg 和单文件构建成功。
- 校验归档 CRC、manifest 入口路径、全部 9 个导出工具，以及重复构建 SHA-256 一致。
- 核心逻辑采用先失败测试再实现；校准节点重复、裁剪视口手势越界及跨会话旧请求均以失败回归测试复现后修复。
- 独立代码审查核对了宿主公开 UI/Java bridge 类型及部分实现。发现缺少 session_id 时旧请求可能绑定新会话，已将四动作 session_id 改为必填并加入回归测试。
- 宿主测试使用边界替身，不能证明 Android Java bridge 文件锁、权限、抖音节点及后台 ASR 在设备上可用。
- 引擎被强制销毁可能跳过 JS finally，文件锁释放需实机验证。持续 BUSY 时停止并重启 Operit，不抢占锁。

实施计划任务 1–4 已完成；任务 5 的测试、构建、审查已完成，提交状态以 Git 记录为准。真机验收使用 docs/android-testing.md，全部实机项仍待测试。
